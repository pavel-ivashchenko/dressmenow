import { CheckIfEqualDirective } from './check-if-equal.directive';

describe('CheckIfEqualDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckIfEqualDirective();
    expect(directive).toBeTruthy();
  });
});
