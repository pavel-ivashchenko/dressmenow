import { HasCapitalCaseDirective } from './has-capital-case.directive';

describe('HasCapitalCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new HasCapitalCaseDirective();
    expect(directive).toBeTruthy();
  });
});
