import { HasNumberDirective } from './has-number.directive';

describe('HasNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new HasNumberDirective();
    expect(directive).toBeTruthy();
  });
});
