import { HasSmallCaseDirective } from './has-small-case.directive';

describe('HasSmallCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new HasSmallCaseDirective();
    expect(directive).toBeTruthy();
  });
});
