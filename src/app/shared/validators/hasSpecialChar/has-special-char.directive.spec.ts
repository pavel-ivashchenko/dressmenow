import { HasSpecialCharDirective } from './has-special-char.directive';

describe('HasSpecialCharDirective', () => {
  it('should create an instance', () => {
    const directive = new HasSpecialCharDirective();
    expect(directive).toBeTruthy();
  });
});
